import React from 'react';

function Header() {

  return (
    <header>
      <h1>Trybe</h1>
      <button onClick={() => {}}>
      {/* {theme.color === 'dark' ? '☀️' : '🌒'} */}
      </button>
    </header>
  );
}

export default Header;
