const INITIAL_STATE = {
  id: '',
};

const selectedPill = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    // implemente aqui
    default:
      return state;
  }
};

export default selectedPill;
